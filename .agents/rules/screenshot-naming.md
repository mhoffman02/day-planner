# Semantic Screenshot Naming

- MUST name screenshot artifacts `<viewport>_<view_or_component>.png` (e.g. `desktop_daily.png`, `modal_event_edit.png`, `search_modal.png`).
- MUST NOT use generic, verbose, or timestamped names (`ui_review_1.png`, `screenshot_20260817_120400.png`).
